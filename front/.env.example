# Frontend Environment Variables (Vite)
# If deploying frontend and backend under different origins, set VITE_API_BASE to the backend origin + "/api"
# Example: VITE_API_BASE=https://api.example.com/api
# In local dev with Vite proxy, you can leave this unset.
VITE_API_BASE=